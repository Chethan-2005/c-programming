#include <stdio.h>

int main() {
    int n;
    float sum;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    sum = (float) n * (n + 1) / 2;

    printf("The sum of first %d numbers is: %f\n", n, sum);

    return 0;
}
